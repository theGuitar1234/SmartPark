[project]
name = "smartpark"
version = "1.1.0"
description = "SmartPark parking operations dashboard with FastAPI, SQLite, and Flet"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

[tool.ruff]
line-length = 140
